# 玄机阁 · 界面 redesign 设计方案

> 在保留「玄机阁」水墨暗金品牌的前提下，针对**易用性**与**用户习惯**做的系统升级。
> 本文档为设计交付物之一，配合 `redesign-preview.html` 高保真预览共同评审。

---

## 1. 设计原则（为什么这么改）

| 维度 | 现状 | 新方案 | 贴合用户习惯的点 |
|------|------|--------|------------------|
| 字体 | 全站衬线（长文/表单偏累） | **标题衬线 + 正文无衬线** | 中文界面正文用无衬线（PingFang/系统）是可读性惯例，长文/表单更省力 |
| 五行表达 | 仅文字提及 | **五色功能标签**（木青·火红·土黄·金白·水蓝） | 八字/喜用「一眼可辨」，命理工具可用性实质提升 |
| 导航 | 顶部纯文字链接 | **吸顶玻璃栏 + 移动端底部标签栏** | 符合 App 导航习惯，移动端单手可达 |
| 首屏 | 居中标题 + 小字 | **印章徽标 + 一键快捷入口** | 降低首屏决策成本（今日宜忌/抽一签/查八字 一触即达） |
| 结果区 | 各页各自为政 | **统一卡片语言 + 状态可见** | 一致性降低认知负荷；离线 pill、焦点环给明确反馈 |

---

## 2. 设计令牌（Design Tokens）

### 色彩
```
背景层次：--ink-0 #0e0c09（最深） → --ink-1 #16120d（基） → --ink-2 #1f1a13（面板） → --ink-3 #282116（浮起）
沉金：--gold #d2a857 / --gold-soft #e8cf94 / --gold-dim #9a7Ⓐ44
朱砂：--cinnabar #b5402f / --cinnabar-soft #df7164
正文：--paper #ece3d2 / 次要 --muted #a99e88 / 细线 --line #3a3020
五行：木 #6fae8e · 火 #c8553d · 土 #d9b96b · 金 #cfc6b0 · 水 #6e8fb0
圆角 --r:14px ｜ 最大宽 --maxw:1080px
```
### 字体
```
标题/品牌：--serif（宋体/楷体，书法感）
正文/控件：--sans（-apple-system, "PingFang SC", "Microsoft YaHei", sans-serif，清晰易读）
```
### 间距 / 动效
- 统一卡片圆角 14px、面板渐变、hover 上浮 + 金边 + 阴影
- 受 `prefers-reduced-motion` 保护，默认淡入上浮（`xj-rise`）

---

## 3. 关键组件规格

### 顶栏（吸顶玻璃）
- `position:sticky; top:0; backdrop-filter:blur(14px)` 半透明背景，导航随时可见
- 品牌区：印章徽标 + 中英双名
- 桌面导航 → 移动端折叠为**底部 5 项标签栏**（含 emoji 图标），单手可达

### Hero（扉页）
- 居中印章徽标（64px 大印）+ 主标题 玄机阁（衬线大字）
- 「今日」实时筹码：干支 + 农历 + 日柱，一眼可读
- 双 CTA：主按钮（朱砂渐变）+ 幽灵按钮（金边）

### 模块宫格（6 卡）
- 分类标签（首批/核心/进阶）置于右上角，信息层级清晰
- 左侧 3px 金线 hover 渐显，呼应「印章落定」
- emoji 图标 + 标题 + 一句描述

### 结果区统一卡片
- 键值对（`.kv`）对齐排版，五行标签用色点 + 边框传达「喜/忌」

---

## 4. 响应式断点
```
≤720px：宫格转 2 列；导航隐藏、改底部标签栏；示例卡片堆叠
≤460px：宫格转单列
```

## 5. 无障碍（A11y）
- 跳转链接（skip link）、`:focus-visible` 焦点环
- AA 对比度（金/朱砂 on 深底）
- 语义化 HTML + 读屏友好

---

## 6. 与现有站点关系
- 本方案是**设计提案**：`redesign-preview.html` 为自包含高保真预览，不改动线上 `index.html` / `style.css`。
- 五行色、字体分层、底部标签栏等可直接迁移到真实站点（届时只需同步 `style.css` 令牌与少量结构）。
- 评审通过后，落地仅需把上述令牌/组件**复制进现有 CSS 并替换对应 HTML 片段**。
